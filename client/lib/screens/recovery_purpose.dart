/// Что именно восстанавливаем — единый флоу (выбор способа → код с почты →
/// финальный шаг) один и тот же для обоих случаев, различается только
/// текст на экранах и то, что происходит на последнем шаге (см.
/// recovery_choose_method_screen.dart и дальше по цепочке).
enum RecoveryPurpose { password, totp }
